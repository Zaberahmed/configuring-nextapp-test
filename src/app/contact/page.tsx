import React from "react";

const Contact = () => {
  // console.log("env test from contact", process.env.NEXT_PUBLIC_SECRET_KEY);
  return (
    <main className="bg-green-500">
      <p>This is the about page</p>
    </main>
  );
};

export default Contact;
